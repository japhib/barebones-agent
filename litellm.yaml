# LiteLLM proxy config for barebones-agent.
# Copy to ~/.barebones-agent/litellm.yaml and fill in the placeholders.
# Reload after editing:  launchctl kickstart -k gui/$(id -u)/com.barebones-agent.litellm

model_list:
  # Run `gcloud auth application-default login` once and LiteLLM picks up the file at
  # ~/.config/gcloud/application_default_credentials.json.
  - model_name: vertex-claude
    litellm_params:
      model: vertex_ai/claude-sonnet-4-5@20250929
      vertex_project: "REPLACE_WITH_GCP_PROJECT"
      vertex_location: us-east5

  - model_name: deepseek
    litellm_params:
      model: deepseek/deepseek-chat
      api_key: os.environ/DEEPSEEK_API_KEY

  - model_name: deepseek-reasoner
    litellm_params:
      model: deepseek/deepseek-reasoner
      api_key: os.environ/DEEPSEEK_API_KEY

  - model_name: claude
    litellm_params:
      model: anthropic/claude-sonnet-5
      api_key: os.environ/ANTHROPIC_API_KEY
      cache_control_injection_points:
        - { location: message, role: system }
        - { location: tool_config }

litellm_settings:
  # The agent sends max_tokens on every request; drop anything an upstream rejects
  # rather than failing the turn.
  drop_params: true
