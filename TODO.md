- simple git tools for readonly stuff - git status, git log -p
- start in plan mode
- tool to tell the LLM which commands are already approved
    - parse |head |grep |tail `2>&1` on bash and allow them automatically if the prefix is allowed.
'Also add another tool for the LLM to check which commands are already approved (auto-approval)'

- output at the beginning when a context file (and which) is included to the LLM
- change -e to the default operation mode
- start in plan mode by default